{{#if @model}}
  <LinkTo
    @route={{@route}}
    @model={{@model}}
    id={{@id}}
    class="button button_{{@color}}"
  >
    {{@label}}
  </LinkTo>
{{else}}
  <LinkTo
    @route={{@route}}
    id={{@id}}
    class="button button_{{@color}}"
  >
    {{@label}}
  </LinkTo>
{{/if}}
